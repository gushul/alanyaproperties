<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns="http://www.w3.org/1999/html">
<head>
  <meta http-equiv="content-type" content="text/html; charset=utf-8"/>
  <title><%= content_for?(:title) ? yield(:title) : "Alanya Properties" %></title>

  <meta name="description" content="<%= yield :description %>"/>
  <% if content_for?(:keywords) %>
    <meta name="keywords" content="<%= yield :keywords %>"/>
  <% end %>

  <%= stylesheet_link_tag "application", :media => "screen, projection" %>
  <!-- что бы телефон не ломался из-за скайпа -->
  <meta name="SKYPE_TOOLBAR" content="SKYPE_TOOLBAR_PARSER_COMPATIBLE"/>

  <%= javascript_include_tag "application" %>
  <%= auto_discovery_link_tag :atom, "/news" %>
  <%= auto_discovery_link_tag :rss, "/news.rss" %>
  <%= csrf_meta_tags %>

  <!--[if IE 7]>
  <script>browser_class = 'ie_7';</script><![endif]-->
  <!--[if IE 8]>
  <script>browser_class = 'ie_8';</script><![endif]-->
  <%= render 'shared/ga' if Rails.env.production? %>
</head>

<body>
<!-- BEGIN wrapper-->
<div class="wrapper min_width">

  <!-- BEGIN header-->
  <div class="header clearfix">
    <%= link_to root_path, class: 'logo', title: 'Alanya Properties' do %>
        <%= image_tag 'logo.png', alt: 'Alanya Properties Logo' %>
    <% end %>
    <table class="nav">
      <tr>
        <td class="td"><%= menu_link "КУПИТЬ", buy_url %></td>
        <td class="td"><%= menu_link "АРЕНДОВАТЬ", rent_url %></td>
        <td class="td"><%= menu_link "УСЛУГИ", services_url %></td>
        <td class="td"><%= menu_link "О ТУРЦИИ", turkey_news_index_url %></td>
        <td class="td"><%= menu_link "О НАС", about_pages_url %></td>
        <td class="td"><%= menu_link "КОНТАКТЫ", contacts_url %></td>
        <td class="td last_td">
          <a class="search_item">ПОИСК</a>

          <div class="search_input" style="display:none;">
            <form action="/search" method="GET">
              <input class="input" id="main-search-input" type="text" name="q"/><i class="search_btn"></i>
            </form>
          </div>
        </td>
      </tr>
    </table>
  </div>
  <!-- END header-->

  <!-- BEGIN content-->
  <div class="content <%= @content_class %>">
    <%= yield %>
  </div>
  <!-- END content-->

  <!-- BEGIN footer-->
  <div class="footer">
    <div class="inner clearfix">
      <ul class="fot_nav">
        <li class="li first"><%= menu_link "Купить", buy_url %></li>
        <li class="li"><%= menu_link "Арендовать", rent_url %></li>
        <li class="li"><%= menu_link "Услуги", services_url %></li>
        <li class="li"><%= menu_link "О Турции", turkey_news_index_url %></li>
        <li class="li"><%= menu_link "О нас", about_pages_url %></li>
        <li class="li"><%= menu_link "Контакты", contacts_url %></li>
        <li class="li"><%= menu_link "Поиск", search_url %></li>
        <li class="li"><%= menu_link "Карта сайта", sitemap_url %></li>
      </ul>
      <div class="fright">
        <i class="fot_separ"></i>

        <div class="phone">Телефон : <%= @global_settings.phone %></div>
        <i class="fot_separ"></i>
        <a class="icon fot_rss" href="<%= turkey_news_index_path %>" title="RSS Feed"></a>
        <i class="fot_separ"></i>
        <a class="icon fot_fb" href="<%= @global_settings.facebook %>" title="Facebook Page" target="_blank"></a>
      </div>
    </div>
    <div class="copyright"><%= @global_settings.copyright %></div>
  </div>
  <!-- END footer-->
</div>
<!-- END wrapper-->


<table class="popup_table hide">
  <tr>
    <td class="pt_td">
      <!-- !!! popup_leaveorder -->
      <!--<script>visible_popup       = 'popup';</script>-->
      <div class="popup clearfix">
        Popup
        <i class="close"></i>
      </div>
      <div class="back none" id="back"></div>

    </td>
  </tr>
</table>

<%= render 'shared/ym' if Rails.env.production? %>
<%= render 'shared/livezilla' if Rails.env.production? %>
</body>
</html>
