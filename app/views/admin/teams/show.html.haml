- model_class = ContactPerson
.page-header
  %h1= t '.title', :default => model_class.model_name.human
%dl.dl-horizontal
  - if @team_member.name?
    %dt
      %strong
        = model_class.human_attribute_name(:name)
        \:
    %dd= @team_member.name
  - if @team_member.phone?
    %dt
      %strong
        = model_class.human_attribute_name(:phone)
        \:
    %dd= @team_member.phone
  - if @team_member.skype?
    %dt
      %strong
        = model_class.human_attribute_name(:skype)
        \:
    %dd= @team_member.skype
  - if @team_member.language?
    %dt
      %strong
        = model_class.human_attribute_name(:language)
        \:
    %dd= @team_member.language
  - if @team_member.position?
    %dt
      %strong
        = model_class.human_attribute_name(:position)
        \:
    %dd= @team_member.position
  - if @team_member.email?
    %dt
      %strong
        = model_class.human_attribute_name(:email)
        \:
    %dd= @team_member.email
  %br
  - if @team_member.photo?
    %dt
      %strong
        = model_class.human_attribute_name(:photo)
        \:
    %dd= image_tag @team_member.photo
.form-actions
  = link_to t('.back', :default => t("helpers.links.back")), |
    admin_manager_people_path, :class => 'btn'               |
  -#
  = link_to t('.edit', :default => t("helpers.links.edit")),         |
    edit_admin_team_member_path(@team_member), :class => 'btn' |
  -#
  = link_to t('.destroy', :default => t("helpers.links.destroy")),                                             |
    admin_team_member_path(@team_member),                                                                |
    :method => 'delete',                                                                                       |
    :data => {:confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?'))}, |
    :class => 'btn btn-danger'
