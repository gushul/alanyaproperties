%h1 Listing s

%table
  %tr
    %th
    %th
    %th
    %th

  - @team_members.each do |member|
    %tr
      %th= member.name
      %td= link_to 'Show', admin_team_path(member)
      %td= link_to 'Edit', edit_admin_team_path(member)
      %td= link_to 'Destroy', admin_team_path(member), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Team', new_admin_team_path
