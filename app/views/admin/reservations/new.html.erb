<%- model_class = Reservation -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.new', 'New %{model} %{for}'], :model => model_class.model_name.human,
    :for => parent? ? "for #{parent.name}" : '' %></h1>
</div>
<%= render :partial => 'form' %>
